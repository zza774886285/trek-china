import type { TranslationStrings } from '../types';

const mobileSettings: TranslationStrings = {
  'mobileSettings.blurBookingCodesHint': 'Ocultar códigos de reserva até você tocar para revelar',
};

export default mobileSettings;
